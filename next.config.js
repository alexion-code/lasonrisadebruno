/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // images: { unoptimized: true },
  // basePath: "/lasonrisadebruno",
  // images: { unoptimized: true },
};

module.exports = nextConfig;
